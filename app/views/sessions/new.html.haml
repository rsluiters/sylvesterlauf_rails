= provide(:title, "Anmelden Interner Bereich")
%h1 Interner Bereich: Anmeldung

.row
  .span6.offset3
    = form_for(:session, url: sessions_path) do |f|
      = f.label :email
      = f.text_field :email
      = f.label :password
      = f.password_field :password
      = f.submit "Anmelden", class: "btn btn-large btn-primary"
    %p
      Kein Account oder passwort vergessen? Dann Ralph Bescheid geben.