<%= form_for(@applications_event) do |f| %>
  <% if @applications_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@applications_event.errors.count, "error") %> prohibited this applications_event from being saved:</h2>

      <ul>
      <% @applications_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :deadline_internal %><br />
    <%= f.date_select :deadline_internal %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :announcement_url %><br />
    <%= f.text_field :announcement_url %>
  </div>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
